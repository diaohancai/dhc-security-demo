server:
  port: 8080

spring:
  profiles:
    active: dev

  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:orcl
    username: dhc
    password: 11111111

  session:
    store-type: none

security:
  basic:
    enabled: false

